version: 0.1

phases:
  install:
    runtime-versions:
        java: openjdk11
  install:
   commands:
     - echo Entering install phase...
     - echo Nothing to do now
  build:
    commands:
     - echo Build started on `date`
     - gradlew compile
    commands:
     - gradlew build
artifacts:
  type: zip
  files:
    - 'target/boot.jar'
